# ByteCraft
A web-based Java code editor that lets users write, compile, and run Java programs directly from the browser. It provides syntax highlighting, error handling, and real-time output. The backend uses Spring Boot to securely compile and execute code in isolated environments.
